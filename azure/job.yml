$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
type: command

inputs:
  lr:
    type: number
    default: 0.01
  epochs:
    type: integer
    default: 5

code: .

command: >
  python src/train.py
  --lr ${{inputs.lr}}
  --epochs ${{inputs.epochs}}

environment: azureml:AzureML-sklearn-1.0-ubuntu20.04-py38-cpu@latest
compute: azureml:cpu-cluster

experiment_name: exp3-params-metrics
description: Experiment 3 – parameters and metrics logging
